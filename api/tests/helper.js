console.log('+++++++++ +++++++ BEFORE +++++++ +++++++++++');
before(function (done) {
  // knex.select('id')
  //   .from('users')
  //   .where({email:'jsnow@winterfell.got'})
  //   .limit(1)
  //   .then(function(rows) {
  //     const headers = { Authorization: createToken(rows[0].id) };
  //     global.idOfJonSnow = rows[0].id;
  //     done();
  //   });
  done();
});
