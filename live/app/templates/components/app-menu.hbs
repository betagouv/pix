<div class="app-menu">
  {{#each items as |menuItem|}}
    {{#each menuItem as |item|}}
      <div class="app-menu__item">
        <a href="{{item.href}}">{{item.title}}</a>
      </div>
    {{/each}}
  {{/each}}
</div>