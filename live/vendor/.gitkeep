it('Should render component with a link to /projet', function(){
    //Given
    addItemToMenu(this,{
      name : ['projet'],
      link : ['/projet']
    });

    //When
    renderAppMenu(this);

    //then
    findWithAssert

    expect($downloadLink.prop('href')).to.equal('http://challenge.file.url/');*/
  });


  it('Should render first item equal to projet in component App-menu', function(){
      //When
      renderAppMenu(this);
      //then
      const firstItem = (this.$('.app-menu__item>a').length>1)? this.$('.app-menu__item>a')[0] : this.$('.app-menu__item>a');
      expect(this.$(firstItem).text()).to.equal('projet');
      expect(this.$(firstItem).prop('href')).to.equal('/projet');
    });
